[project]
name = "hegel-cpp-conformance"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "hegel",
    "pytest",
]

[tool.uv.sources]
hegel = { git = "https://github.com/hegeldev/hegel-core" }
