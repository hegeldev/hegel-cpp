[project]
name = "hegel-cpp-conformance"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "hegel",
    "pytest",
]

[tool.uv.sources]
hegel = { git = "ssh://git@github.com/antithesishq/hegel" }
